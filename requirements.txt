# S&P SL 20 forecasting — Python dependencies
# Tested with Python 3.14. The LSTM uses PyTorch (TensorFlow has no 3.14 wheel);
# the architecture is identical to a Keras 2x50-unit LSTM.
#
# Setup:
#   python -m venv .venv
#   source .venv/bin/activate        # Windows: .venv\Scripts\activate
#   pip install -r requirements.txt

pandas==3.0.3
numpy==2.5.1
matplotlib==3.11.1
statsmodels==0.14.6
pmdarima==2.1.1
scikit-learn==1.9.0
torch==2.13.0
xgboost==3.3.0          # macOS also needs the OpenMP runtime: `brew install libomp`
reportlab==5.0.0
pypdf==6.14.2
